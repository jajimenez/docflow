# Contributing

## Branching strategy

This project follows **GitHub Flow**:

- `main` is always deployable
- Create a short-lived feature branch for each change: `feature/change`
- Open a pull request to merge into `main`
- Delete the branch after merging

## Commit messages

Use short, imperative messages: `Add X`, `Fix Y`, `Remove Z`.
